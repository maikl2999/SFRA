<h1>Product type</h1>

<h2>${pdict.name}</h2>
<div>
    <ul>
        <isloop items="${pdict.ids}" var="id">
            <li><span>id: ${id}</span></li> 
        </isloop>
    </ul>
    <iscomment> ${pdict.variants} </iscomment>
</div>